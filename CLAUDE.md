# Portfolio — Project Instructions

Personal portfolio site for Rodrigo Camino. Astro + Tailwind, EN/ES i18n, deployed on Vercel.

## Active project: full redesign (4 phases)

This site is mid-redesign. **Before doing any work, read the orchestration hub:**

→ `docs/superpowers/ROADMAP.md` — current phase, live status, how to resume.

It links to the design spec, the per-phase plans, and the supporting audits.
Redesign work happens on the `redesign` branch, not `main`.

## Conventions

- Package manager: **pnpm** (`pnpm dev`, `pnpm build`, `pnpm astro check`, `pnpm test`).
- Git user is per-project: `Rodrigo Camino` / `rodrigo.camino97@gmail.com`.
- When delegating to subagents, follow `docs/prompts/subagents-directive.md`.
- Commit or push only when asked.
